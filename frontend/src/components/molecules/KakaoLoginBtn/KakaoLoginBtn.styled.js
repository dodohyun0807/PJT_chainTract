import styled from '@emotion/styled';

const MainContainer = styled.div`
  color: #000;
  cursor: pointer;
  font-size: 1.3rem;
  font-family: arial;
  :hover {
    transform: scale(1.1);
    transition: 1s;
  }
`;

export default {
  MainContainer,
};
